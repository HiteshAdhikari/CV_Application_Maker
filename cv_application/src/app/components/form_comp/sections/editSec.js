const EditSec = ({ children }) => {
  return (
    <section className="mt-2 flex justify-between w-full bg-slate-200 font-semibold rounded-md p-1   ">
      {children}
    </section>
  );
  // gap-16
};
export default EditSec;
